name: Schedule canary

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: none

jobs:
  tick:
    runs-on: ubuntu-latest
    steps:
      - run: echo "scheduler alive at $(date -u)"
